apiVersion: v1
kind: Service
metadata:
  name: {{ template "helm-gitfocus.fullname" . }}
  labels:
    app: {{ template "helm-gitfocus.name" . }}
    chart: {{ template "helm-gitfocus.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "helm-gitfocus.name" . }}
    release: {{ .Release.Name }}
